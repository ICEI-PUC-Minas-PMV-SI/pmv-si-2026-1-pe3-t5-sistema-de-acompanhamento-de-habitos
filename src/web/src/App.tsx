export default function App() {
  return (
    <div className="min-h-screen bg-bg text-text p-8">
      <h1 className="font-display text-3xl text-primary">SAH</h1>
      <p className="text-textMuted">Scaffold + Tailwind + tokens ok.</p>
      <button className="mt-4 bg-primary hover:bg-primaryHover text-surface rounded-md px-4 py-2 shadow-sm">
        Botão teste
      </button>
    </div>
  );
}
