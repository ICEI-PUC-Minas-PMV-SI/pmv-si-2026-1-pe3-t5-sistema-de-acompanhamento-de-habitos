abstract final class SahDurations {
  static const fast = Duration(milliseconds: 150);
  static const normal = Duration(milliseconds: 300);
  static const bounceIn = Duration(milliseconds: 400);
  static const progressBar = Duration(milliseconds: 500);
  static const spinner = Duration(milliseconds: 700);
  static const slow = Duration(milliseconds: 1400); // shimmer
}
